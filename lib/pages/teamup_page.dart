import 'package:flutter/material.dart';

class TeamUpPage extends StatelessWidget {
  const TeamUpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("TeamUp Page")),
    );
  }
}
